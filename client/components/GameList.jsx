import React from 'react';


const GameList = ((props)=>{
    const {user_id} = props;
    console.log(user_id)
    return(
        <div>
            <h1>This is to be GameList</h1>
        </div>
    )

})

export default GameList